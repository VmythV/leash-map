# tests — Cross-module & End-to-End Tests

跨模块与端到端自动化测试。单模块的单元测试就近放在各自目录内
（如 `server/`、`simulator/`、`app/`）；此处聚焦系统闭环验收。

## MVP 验收闭环（参考 docs/TODOLIST.md Phase 12）

1. 模拟器持续上传位置。
2. 云端存储并可查询位置点。
3. App 显示最新位置。
4. App 回放历史轨迹。
5. 创建圆形安全区。
6. 设备离区触发告警。
7. 低电 / 离线告警可展示。
8. 寻宠模式提升上报频率。
9. 离线缓存重连后补传。
